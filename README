== Welcome to SObjectBrowser Rails App.

SObjectBrowser is a rails app that uses database-com heroku addon and lets you
browse the available SObjects in a provisioned database.com Org. It uses JQuery Mobile for UI.
